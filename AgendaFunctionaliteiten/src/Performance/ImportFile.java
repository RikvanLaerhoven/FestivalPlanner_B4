package Performance;

public class ImportFile {
    public void importFile(Artist artist){
        if(GUI.importFile == file1) {
            file1 = SaveFile.getSavelist1;
        }
        if(GUI.importFile == file2) {
            file2 = SaveFile.getSavelist2;
        }
        if(GUI.importFile == file3) {
            file3 = SaveFile.getSavelist3;
        }
        if(GUI.importFile == file4) {
            file4 = SaveFile.getSavelist4;
        }
    }
}
